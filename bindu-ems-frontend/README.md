# Bindu EMS — Frontend

React + Vite frontend that connects to an existing Flask + SQLAlchemy + MySQL backend.

## Setup

```bash
npm install
cp .env.example .env       # then edit VITE_API_URL
npm run dev